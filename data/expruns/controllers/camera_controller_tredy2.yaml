# Camera configuration for tredy2. Run Verify_Cameras.ipynb after changing
# hardware, then map each stable logical view name to its current device index.
views:
  camera_0:
    device: 0
  camera_1:
    device: 2

saved_image_size: [512, 512]
fps: 10
visualize: true
